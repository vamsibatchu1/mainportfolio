/**
 * List of routes that need to be rendered dynamically
 * to prevent "document is not defined" errors during build
 */
export const dynamicPaths = [
  '/',
  '/work',
  '/home',
  '/writing',
  '/experiments',
  '/about',
  '/blog',
  '/contact',
  '/casestudy'
]; 